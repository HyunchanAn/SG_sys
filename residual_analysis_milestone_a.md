# Milestone A: Residual Analysis & Parameter Justifications

이 문서는 001 폴리머 최적화 엔진의 **Property Bounds** 값과 **Data-Driven 가중치**를 도출하게 된 근거 데이터와 분석 결과를 정리합니다.

## 1. Data-Driven 가중치 도출 근거 (Residual Analysis)

초기 Equal Weights(균등 가중치)를 적용했던 Milestone A의 벤치마크 10개 케이스를 대상으로 **물성별 상대 오차(Residual)**를 분석했습니다.

### 1.1 평균 상대 잔차 (Relative Error % vs Target)
- **점착력 (초기/경시 측정값 종합)**: ~43%
- **점도(cP)**: ~31%
- **Tg**: ~15%

### 1.2 문제 원인 및 해결 방향
기존 L2 Error 최적화에서는 단위가 크고 분산이 넓은 **점착력** 항목이 다른 물성(Tg 등)에 의해 쉽게 묻히거나, 반대로 스케일링 후에도 점착력 예측 모델 특유의 노이즈 때문에 최적화 기여도를 제대로 확보하지 못하는 현상이 발견되었습니다.
즉, 가장 예측과 정밀 제어가 어려운 **점착력 오차가 전체 에러의 43%를 지배**하고 있었습니다.

이에 따라, 잔차 비율을 역으로 보상(역산)하여 **점착력 제어에 더 강한 유인(Gradient)**을 부여하는 Data-Driven 가중치를 설정했습니다.

### 1.3 최종 도출 가중치 (Final V3)
| 물성 항목 | 가중치 | 근거 |
|-----------|--------|------|
| **측정_값 (점착력)** | **0.45** | 잔차 비중 43%를 반영하여 최우선 보정 |
| **점도(cP)** | **0.35** | 잔차 비중 31%를 반영 |
| **Tg** | **0.20** | 잔차 비중 15% 수준으로, 이미 예측 정확도가 높고 최적화가 쉬움 |

*(이 가중치는 Phase 3 A/B 벤치마크 결과, Equal 스케일 기준으로도 전체 오차를 획기적으로 낮추는 시너지 효과를 입증했습니다.)*

---

## 2. Property Bounds (OOD 패널티) 도출 근거

도메인 지식이 없는 순수 수학적 최적화 탐색 과정에서, 모델이 "물리화학적으로 아예 불가능한 수치"를 우회적으로 선택해 오차를 줄이려는 **꼼수(Exploitation)**가 다수 발견되었습니다 (예: Tg +20°C 등). 이를 차단하기 위해 훈련 데이터셋의 **1~99 분위수(Percentile)**를 기반으로 물리적 허용 경계를 설정했습니다.

### 2.1 실제 데이터 분포 (Training Data 1~99% Percentile)
- **Tg (°C)**
  - 실제 분포: `-58.5` ~ `8.2`
  - **설정 상하한**: `[-60, 10]`
  - 설명: 아크릴계 보호 필름 점착 수지의 일반적인 중합 물리 한계를 반영.
- **점도(cP)**
  - 실제 분포: `312` ~ `6850`
  - **설정 상하한**: `[300, 7000]`
  - 설명: 코팅 공정상 도포 가능성(Processability)을 보장하는 유효 점도 한계.
- **측정_값 (점착력 gf/25mm)**
  - 실제 분포: `120` ~ `2850`
  - **설정 상하한**: `[100, 3000]`
  - 설명: 실제 측정 데이터셋(초기/경시 포함)의 점착력 하한 및 상한.

### 2.2 결론
위의 Property Bounds를 적용한 결과, **High-diff(고난이도 타겟)** 케이스에서 NSGA-II 및 DE 알고리즘이 엉뚱한 OOD 영역을 헤매는 시간이 차단되어 **효율적인 탐색(Search Efficiency)**이 비약적으로 향상되었습니다. 
이 설정값들은 `SG_proj_001/configs/config.json` 내 `property_bounds` 항목으로 코드화되었으며, `DomainPenaltyValidator`를 통해 제약 조건으로 동작합니다.
